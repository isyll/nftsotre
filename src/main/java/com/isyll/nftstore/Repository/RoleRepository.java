package com.isyll.nftstore.Repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.isyll.nftstore.Model.Role;

public interface RoleRepository extends JpaRepository<Role, Long> {

}
